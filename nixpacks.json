{
  "build": {
    "cmd": "npm install && npm run docs:build"
  },
  "start": {
    "cmd": "npx serve docs/.vitepress/dist -p $PORT"
  },
  "outputs": ["docs/.vitepress/dist"],
  "variables": {
    "NODE_VERSION": "18"
  }
}
